# API keys (do not commit real values). The framework does NOT autoload this
# file; source it into your shell session before running the CLI.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Optional: hard budget cap in USD; overrides the suite's max_cost_usd.
# KHEDRON_MAX_BUDGET_USD=15
